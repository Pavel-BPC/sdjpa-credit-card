spring:
  datasource:
    url: jdbc:mysql://localhost:3306/payment?useUnicode=true&characterEncoding=UTF-8&serverTimezone=UTC
    password: password
    username: paymentuser
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: validate
  sql:
    init:
      mode: always
  flyway:
    user: paymentadmin
    password: password

